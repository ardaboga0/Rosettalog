# XSIAM case 01 (X01)

See [the pack README](../README.md) for the procedure and
[`unknowns.yaml`](../../../../src/rosettalog/backends/xsiam/unknowns.yaml) for the question.

- Vendor / product: `rosettalog` / `rl_x01` (dataset `rosettalog_rl_x01_raw`).
- Rule: [`rule.xif`](rule.xif). Lines: [`sample.log`](sample.log).
- Record: `matched`, `user` and `opt` for each line.
- Expected under the current assumption: [`expected.yaml`](expected.yaml).
