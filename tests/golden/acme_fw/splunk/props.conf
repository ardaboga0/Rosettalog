# Generated by rosettalog from acme_fw.lsx.xml
# Review the migration report before deploying. Do not edit by hand; regenerate.

[acme:firewall]
# ---- Index-time settings -------------------------------------------------------
# Deploy to indexers / heavy forwarders (the first full instance parsing the data).
# They affect ONLY events indexed after deployment; indexed data is not changed.
SHOULD_LINEMERGE = false
TIME_PREFIX = ts="
TIME_FORMAT = %Y-%m-%d %H:%M:%S.%3N
# ---- Search-time extractions ---------------------------------------------------
# Deploy to search heads. They apply at search time to all events of this
# sourcetype, including data indexed before deployment.
REPORT-rl_acme_fw_lsx = rl_acme_fw_lsx_1, rl_acme_fw_lsx_2, rl_acme_fw_lsx_3, rl_acme_fw_lsx_4, rl_acme_fw_lsx_5, rl_acme_fw_lsx_6, rl_acme_fw_lsx_7, rl_acme_fw_lsx_8, rl_acme_fw_lsx_9, rl_acme_fw_lsx_10, rl_acme_fw_lsx_11, rl_acme_fw_lsx_12
EVAL-user = coalesce(rl_7_User, rl_8_AccountFallback)
EVAL-category = case(signature_id=="deny", "Firewall Deny", signature_id=="allow", "Firewall Permit", true(), rl_12_Category)
EVAL-severity_id = case(signature_id=="deny", "7", signature_id=="allow", "3", true(), null())
